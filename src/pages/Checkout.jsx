import CheckoutComponent from '../components/user/CheckoutComponent';
import Layout from '../masterlayout/Layout';

const Checkout = () => {
    return (
        <Layout>
            <CheckoutComponent />
        </Layout>
    );
};

export default Checkout;